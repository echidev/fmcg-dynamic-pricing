# AGENTS

## Repo state
- `README.md` has no setup or run commands; there are no defined build/test/lint/typecheck scripts in the repo.
- `src/` only contains `src/placeholder.py`; there is no verified entrypoint or CLI to run.    

## Data and artifacts
- `.gitignore` ignores `data/` and `models/` (plus common model artifact extensions), so generated datasets/models will not be committed by default.

## Existing instructions
- No other instruction files found (`AGENTS.md`, `CLAUDE.md`, `.cursor/rules/`, `.cursorrules`, `.github/copilot-instructions.md`, `opencode.json`).
